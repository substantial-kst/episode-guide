import * as React from 'react';
import { css } from '@emotion/core';

interface Props {
  name: string;
}

const color = 'white';

const Detail = ({ name }: Props) => {
  return (
    <div>
      Search Page
    </div>
  );
};

export default Detail;
